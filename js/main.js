// GRID DESIGNER

// Global Constants
const NUM_ROWS = 10;
const NUM_COLS = 10;

// Create array to represent a grid
let temp = createEmptyArray();
randomizeArray(temp)
let grid = temp;



// Create divs to model the grid array
createDivGrid(grid);


